node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Installed by `bun run install:skills`; Kise's own skills remain tracked.
.agents/skills/remotion-best-practices
skills-lock.json

# Agent scratch: worktrees (multi-GB node_modules) and the symlinked skills dir.
.claude/
